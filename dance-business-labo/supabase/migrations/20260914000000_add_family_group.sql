-- 兄弟・親子など家族関係にある生徒を、生徒管理でひと目でわかるようにする。
-- 正規化したテーブルにはせず、他の項目（助成制度名など）と同じくシンプルな自由記述の
-- グループ名で管理する（例: 「山田家」）。同じ名前を入力した生徒同士が家族として表示される。

alter table students add column if not exists family_group text;
